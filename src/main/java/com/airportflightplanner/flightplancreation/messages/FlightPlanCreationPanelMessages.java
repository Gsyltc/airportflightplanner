/*
 * @(#)FlightPlanCreationPanelMessages.java
 *
 * Goubaud Sylvain
 * Created : 2016
 * Modified : 4 août 2016.
 *
 * This code may be freely used and modified on any personal or professional
 * project.  It comes with no warranty.
 *
 */

package com.airportflightplanner.flightplancreation.messages;

import fr.gsyltc.framework.utils.internationalizer.AbstractMessages;

/**
 * @author Goubaud Sylvain
 *
 */
public class FlightPlanCreationPanelMessages extends AbstractMessages {
    
    
    /** */
    private static final String PREFIX = FlightPlanCreationPanelMessages.class.getSimpleName() + ".";

    /** */
    public static final String SCHEDULE_LABEL = format(PREFIX + "SCHEDULE" + LABEL);
    /** */
    public static final String START_LABEL = format(PREFIX + "START" + LABEL);
    /** */
    public static final String END_LABEL = format(PREFIX + "END" + LABEL);
    /** */
    public static final String TIME_LABEL = format(PREFIX + "TIME" + LABEL);
    /** */
    public static final String CALLSIGN_LABEL = format(PREFIX + "CALLSIGN" + LABEL);
    /** */
    public static final String TYPE_LABEL = format(PREFIX + "TYPE" + LABEL);
    /** */
    public static final String CPIE_LABEL = format(PREFIX + "CPIE" + LABEL);
    /** */
    public static final String ROUTE_LABEL = format(PREFIX + "ROUTE" + LABEL);
    /** */
    public static final String DEPATURETYPE_LABEL = format(PREFIX + "DEPATURETYPE" + LABEL);
    /** */
    public static final String ARRIVALTYPE_LABEL = format(PREFIX + "ARRIVALTYPE" + LABEL);
    /** */
    public static final String LANDINGLIGHT_LABEL = format(PREFIX + "LANDINGLIGHT" + LABEL);
    /** */
    public static final String ROUTE_PANEL_LABEL = format(PREFIX + "ROUTE_PANEL" + LABEL);
    /** */
    public static final String MARKER_START = format(PREFIX + "MARKER_START" + LABEL);
    /** */
    public static final String MARKER_END = format(PREFIX + "MARKER_END" + LABEL);
    /** */
    public static final String OPTIONS_LABEL = format(PREFIX + "OPTIONS" + LABEL);
    /** */
    public static final String FLIGHT_INFOS_LABEL = format(PREFIX + "FLIGHT_INFOS" + LABEL);
    /** */
    public static final String AIRCRAFT_LIVERY_LABEL = format(PREFIX + "AIRCRAFT_LIVERY" + LABEL);
    /** */
    public static final String FLIGHTTYPE_LABEL = format(PREFIX + "FLIGHTTYPE" + LABEL);
    /** */
    public static final String FLY_TO_COMPLETION_LABEL = format(PREFIX + "FLY_TO_COMPLETION" + LABEL);
    /** */
    public static final String STEERPOINTS_TITLE = format(PREFIX + "STEERPOINTS" + TITLE);
    /** */
    public static final String FLIGHTSLIST_TITLE = format(PREFIX + "FLIGHTSLIST" + TITLE);
    /** */
    public static final String STARTDAYS_TITLE = format(PREFIX + "STARTDAYS" + TITLE);
    /** */
    public static final String MAP_TITLE = format(PREFIX + "MAP" + TITLE);
    /** */
    public static final String AIRPORT_TITLE = format(PREFIX + "AIRPORT" + TITLE);
}
